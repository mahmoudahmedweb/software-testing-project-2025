# software-testing-project-2025
Project to demonstrate using Cucumber and Selenium Webdriver for test automation.

The test uses the Saucedemo site.

Built using Java.

Run the tests using the following Maven command:
mvn test
